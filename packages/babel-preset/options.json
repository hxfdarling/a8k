{
  "type": "object",
  "required": ["target"],
  "properties": {
    "target": {
      "type": "string",
      "enum": ["node", "web"]
    },
    "useBuiltIns": {
      "oneOf": [
        {
          "type": "string",
          "enum": ["usage", "entry"]
        },
        {
          "type": "boolean",
          "enum": [false]
        }
      ]
    },
    "useESModules": {
      "type": "boolean"
    },
    "modules": {
      "oneOf": [
        {
          "type": "string",
          "enum": ["commonjs"]
        },
        {
          "type": "boolean",
          "enum": [false]
        }
      ]
    }
  }
}
